[phases.setup]
nixPkgs = [
  "python311",
  "python311Packages.pip",
  "libjpeg",
  "libpng",
  "freetype",
  "zlib",
  "libwebp",
  "postgresql",
  "dejavu_fonts",
  "fontconfig"
]

[phases.install]
cmds = ["pip install --break-system-packages -r bot/requirements.txt"]

[start]
cmd = "cd bot && python bot.py"
